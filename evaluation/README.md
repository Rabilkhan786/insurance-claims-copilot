# Evaluation

This directory is independent from the API. It includes executable local metrics for context precision, context recall, faithfulness, and answer relevancy.

Run the included labelled sample with `python -m evaluation.run`. Provide a similarly shaped JSON array using `--dataset` to evaluate your own retrieval and answer outputs.
